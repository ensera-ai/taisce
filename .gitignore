# Build output
/taisce
/dist/
# The documentation site: the staged documents and navigation the generator writes, the renderer's
# cache and output, and its installed dependencies (pinned by site/package-lock.json).
/site/docs/
/site/sidebars.generated.json
/site/build/
/site/.docusaurus/
*.test
*.out

# Local configuration and secrets
.env
.env.local
*.pem
*.key

# Tooling
.DS_Store
node_modules/
__pycache__/
.venv/

# The coverage profile the test target writes. The floor it is checked against is committed;
# the profile is a build artefact.
coverage.out

# Local overrides for the compose stack.
.env.local
# Disposable GPU qualification credentials, generated topology and evidence.
/qualification-tls/
/qualification-topology.yaml
/qualification-generation-nginx.conf
/results/

# Generated by the code-intelligence indexer on each analyze; see the last section of AGENTS.md.
.gitnexus/
.claude/skills/gitnexus/
